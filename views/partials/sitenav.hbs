    <div class="container-fluid bg-light position-relative shadow">
        <nav class="navbar navbar-expand-lg bg-light navbar-light py-3 py-lg-0 px-0 px-lg-5">
            <a href="/" class="navbar-brand font-weight-bold text-secondary" style="font-size: 50px;">
                <i class="flaticon-043-teddy-bear"></i>
                <span class="text-primary">Smart Kids</span>
            </a>
            <button type="button" class="navbar-toggler" data-toggle="collapse" data-target="#navbarCollapse">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse justify-content-between" id="navbarCollapse">
                <div class="navbar-nav font-weight-bold mx-auto py-0">
                        <a href="/" class="nav-item nav-link {{#if isHome}} active {{/if}}">Home</a>
                        <a href="/about" class="nav-item nav-link {{#if isAbout}} active  {{/if}}">About</a>
                        <a href="/class" class="nav-item nav-link  {{#if isClass}} active {{/if}}">Classes</a>
                        <a href="/team" class="nav-item nav-link {{#if isTeam}} active {{/if}}">Teachers</a>
                        {{!-- <a href="/gallery" class="nav-item nav-link {{#if isGallery}} active {{/if}}">Gallery</a> --}}
                        <div class="nav-item dropdown">
                            {{!-- <a href="#" class="" data-toggle="dropdown">Pages</a> --}}
                             <a href="" class="dropdown-toggle nav-link {{#if isGallery}} active {{/if}}">
                             Gallereya
                             </a>
                            <div class="dropdown-menu rounded-0 m-0">
                                {{#each menu}}
                                <a href="/gallery/{{_id}}" class="dropdown-item">{{title}}</a>
                                {{/each}}
                            </div>
                        </div>
                        <a href="/contact" class="nav-item nav-link {{#if isContact}} active {{/if}}">Contact</a>
                </div>
                {{#if isUser}}
                    <a href="/team" class="btn btn-primary px-4">Kirish</a>
                {{else}}                
                    <button class="btn btn-primary px-4" id="auth">Kirish</button>
                {{/if}}
            </div>
        </nav>
    </div>
<div class="modal1 login">
    <div class="modal_box">
        <div class="modal_exit"></div>
        <form action="/admin/auth/user/login" method="post">
        <input type="hidden" name="_csrf" value="{{csrf}}">
            <div class="form-group">
                <label for="exampleInputPassword1">Telefon raqam</label>
                <input type="number" name="phone" class="form-control" id="exampleInputPassword1" placeholder="Telefon raqam">
            </div>
            <div class="form-group">
                <label for="exampleInputPassword1">Password</label>
                <input type="password" name="password" class="form-control" id="exampleInputPassword1" placeholder="Password">
            </div>
            <button type="submit" class="btn btn-primary w-100">Kiritish</button>
            <p class="btn text-center w-100 mt-2" id="regbtn">Ro'yhatdan o'tish</p>
        </form>
    </div>
</div>
<div class="modal1 reg">
    <div class="modal_box">
        <div class="modal_exit exit1"></div>
        <form action="/admin/auth/reg" method="post">
        <input type="hidden" name="_csrf" value="{{csrf}}">
            <div class="form-group">
                <label for="exampleInputEmail1">Ismingiz</label>
                <input type="text" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp" name='name' placeholder="Ismingiz">
            </div>
            <div class="form-group">
                <label for="exampleInputEmail1">Familiyangiz</label>
                <input type="text" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp" name='lname' placeholder="Familiyangiz">
            </div>
            <div class="form-group">
                <label for="exampleInputPassword1">Telefon raqam</label>
                <input type="number" name="phone" class="form-control" id="exampleInputPassword1" placeholder="Telefon raqam">
            </div>
            <div class="form-group">
                <label for="exampleInputPassword1">Password</label>
                <input type="password" name="password" class="form-control" id="exampleInputPassword1" placeholder="Password">
            </div>
            <button type="submit" class="btn btn-primary w-100">Kiritish</button>
        </form>
    </div>
</div>
{{#if error}}
    <div class="toast1 alert alert-danger text-center" role="alert">{{error}}</div>
{{/if}}
{{#if success}}
    <div class="toast1 alert alert-success text-center" role="alert">{{success}}</div>
{{/if}}
<script>
    let auth = document.getElementById('auth');
    let regbtn = document.getElementById('regbtn');
    auth.addEventListener('click', function(){
        document.querySelector('.modal1.login').classList.add('active');
    })
    let exit = document.querySelector('.modal_exit');
    exit.addEventListener('click', function(){
        document.querySelector('.modal1.login').classList.remove('active');
    })
    let exit1 = document.querySelector('.modal_exit.exit1');
    exit1.addEventListener('click', function(){
        document.querySelector('.modal1.reg').classList.remove('active');
    })
    regbtn.addEventListener('click', function(){
        document.querySelector('.modal1.login').classList.remove('active');
        document.querySelector('.modal1.reg').classList.add('active');
    })
    
</script>
