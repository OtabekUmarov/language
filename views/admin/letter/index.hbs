<div class="row">
  <div class="col">
    <div class="card">
      <div class="card-body">
        <div class="d-flex mb-4 justify-content-between" style="align-items: center; ">
          <h5 class="card-title" style="margin: 0;">{{title}}</h5>
        </div>
        <table class="table table-hover">
          <thead>
            <tr>
              <th width="1%">№</th>
              <th scope="col">Nomi</th>
              <th></th>
            </tr>
          </thead>
          <tbody>
            {{#each languages}}
            <tr>
              <td>{{increment @index}}</td>
              <td>{{title}}</td>
              <td><a href="/admin/letter/{{_id}}" class="badge bg-success">Harf qo'shish</a></td>
            </tr>
            {{/each}}
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
