<section class="grey section text-page">
  <div class="container">
    <div class="container-md">
       <table class="table table-hover">
          <thead>
            <tr>
              <th width="1%">№</th>
              <th scope="col">Koreys harfi</th>
              <th scope="col">O'zbekcha</th>
              <th></th>
            </tr>
          </thead>
          <tbody>
            {{#each list}}
            <tr>
              <td>{{increment @index}}</td>
              <td>{{title}}</td>
              <td>{{description}}</td>
              <td>
                <button class="btn btn-primary" onclick="speaker('{{title}}')">Harf talaffuzi</button>
              </td>
            </tr>
            {{/each}}
          </tbody>
        </table>
    </div>
  </div>
</section>

<script>
  function stopSpeaker(){
    window.speechSynthesis.cancel();
  }
  function speaker(value) {
    const select = 'ko-KR'
    const text = value;
    speak(text, select);
    function speak(text, language) {
      window.speechSynthesis.cancel(); 
      const speechMsg = new SpeechSynthesisUtterance();
      speechMsg.lang = language; 
      speechMsg.text = text; 
      window.speechSynthesis.speak(speechMsg); 
    };
  }
</script>
